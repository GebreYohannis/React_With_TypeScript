import { Form } from "./";

const LoginForm = () => {
  return (
    <main className="form-signin  w-100  m-auto rounded-0 border-success">
      <h2 className="text-muted">Login Form</h2>
      <Form />
    </main>
  );
};

export default LoginForm;
